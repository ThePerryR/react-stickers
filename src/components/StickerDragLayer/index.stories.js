import React from 'react'
import { storiesOf } from '@storybook/react'

import StickerDragLayer from './'

storiesOf('components', module)
  .add('StickerDragLayer', () => (
    <StickerDragLayer/>
  ))
